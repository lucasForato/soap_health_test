import React from "react";

interface Props {
  name: string;
  phoneNumber: string;
  id: string;
}

export const PhoneBookItem: React.FC<Props> = ({ name, phoneNumber, id }) => {
  return (
  <div className="">
    
  </div>
  );
};
